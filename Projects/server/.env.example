DB_USER=sa
DB_PASSWORD=yourStrong(!)Password
DB_SERVER=localhost
DB_DATABASE=centerdb
DB_PORT=1433
NODE_ENV=development
PORT=4000
# JWT secret for signing tokens - set this to a long random string in production
JWT_SECRET=replace_this_with_a_strong_secret
# Token expiry (e.g., 7d, 1h)
JWT_EXPIRES_IN=7d
# bcrypt salt rounds
SALT_ROUNDS=10
